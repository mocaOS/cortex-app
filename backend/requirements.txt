# Full dependency set (default build) = base + local ML stack.
# The split exists so the slim image variant (Dockerfile.prod build arg
# INSTALL_LOCAL_ML=false) can drop torch/sentence-transformers/docling
# (~800MB-1GB) from tenant containers that use the shared cortex-helper.
-r requirements-base.txt
-r requirements-ml.txt
